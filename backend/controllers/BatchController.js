const mongoose = require('mongoose');

export const createBatch = (req, res) => {
    
}

export const getBatch = (req, res) => {
    res.send('getBatch')
}

export const deleteBatch = (req, res) => {
    res.send('deleteBatch')
}